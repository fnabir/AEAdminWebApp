export type breadcrumbItem = {
    text: string,
    link?: string,
}

export type options = {
  value: string,
  label?: string,
}

export type expenseDataType = {
  id: number,
  details: string,
  value: number,
}